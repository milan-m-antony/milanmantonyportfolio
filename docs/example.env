# Example Environment Variables
# Copy this file to ' .env.local ' in the project root for Next.js frontend variables.
# For backend/Supabase CLI/Edge Function variables, manage them as described in 01_ENVIRONMENT_SETUP.md.

# Required for Next.js frontend to connect to Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Required for Supabase Admin/Service operations (e.g., in Edge Functions or backend scripts)
# For deployed Edge Functions, set these as secrets in the Supabase Dashboard.
SUPABASE_SERVICE_ROLE_KEY=

# Database password (primarily for local Supabase development)
# This is often set during `supabase init` or `supabase start` and stored internally by the CLI.
# If you need to reference it directly in scripts, ensure it's managed securely.
SUPABASE_DB_PASSWORD=postgres

# Add any other custom environment variables your application needs below:
# Example: OPENAI_API_KEY=
# Example: RESEND_API_KEY=
# Example: NEXT_PUBLIC_SITE_URL=http://localhost:3000 